# Environment
.env
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Application data (keep the schema + generators, ignore live data)
data/mindmate.db
data/*.sqlite

# Trained model artifacts are regenerated by scripts/train_models.py
# (kept out of git to avoid large binaries; run training after clone)
models/artifacts/*.joblib

# OS / IDE
.DS_Store
.vscode/
.idea/
